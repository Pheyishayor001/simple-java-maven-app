pipeline {
  agent {
    label 'server1'
  }
  stages {
    stage('build') {
      steps { 
        sh 'mvn build package'
      }
      post {
      success {
          archiveArtifacts artifacts: '**/target/*.war'
              }
            }
    }    
  }
}
